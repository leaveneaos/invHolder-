<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rjxx.taxeasy.dao.SkpMapper">

    <select id="findOneByParams" parameterType="map" resultType="skp">
        select * from t_skp where yxbz = '1'
       <if test="kpddm != null and kpddm != ''">
            and kpddm = #{kpddm}
        </if>
         <if test="gsdm != null and gsdm != ''">
            and gsdm = #{gsdm} 
        </if>
        limit 1
    </select>

    <select id="findAllByParams" parameterType="map" resultType="skp">
        select * from t_skp where yxbz = '1'
        <if test="orderBy != null">
            order by #{orderBy}
        </if>
    </select>

    <select id="findByPage" parameterType="pagination" resultType="skp">
        select * from t_skp where yxbz = '1'
        <if test="params.orderBy != null">
            order by #{params.orderBy}
        </if>
    </select>

    <select id="getKpd" parameterType="map" resultType="skp">
        select * from t_skp where yxbz = '1' 
		and id in (select kpdid from t_cszb where yxbz='1' and csz='是' and
		kpdid in (SELECT id FROM t_skp WHERE yxbz='1'
 		<if test="xfid != null">
 			and xfid = #{xfid}
 		</if>
 		<if test="xfsh != null">
 			and xfid = (select min(id) from t_xf where xfsh=#{xfsh} and yxbz='1')
 		</if>
 		))
    </select>     

</mapper>

